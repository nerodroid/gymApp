import {Text, View} from 'react-native';

const AppRoot: React.FC = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{fontSize: 25, color: 'black'}}>App Root Screen</Text>
    </View>
  );
};
export default AppRoot;
